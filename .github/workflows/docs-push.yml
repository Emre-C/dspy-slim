name: Docs workflow disabled

on:
  workflow_dispatch:

jobs:
  docs-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm docs workflow is disabled for dspy-slim
        run: echo "In-repo docs publishing is intentionally disabled for dspy-slim."
